<!DOCTYPE html>
<html lang="en">
<head> <!-- Title -->
    <title>Record Donation - Ella Rises</title>
    <%- include('partials/head') %>
</head>
<body class="bg-light">
 <!-- Header -->
    <%- include('partials/header') %>

    <div class="container mt-5 mb-5" style="max-width: 800px;">
        <h1 class="fw-bold text-primary mb-4" style="font-family: 'DM Serif Display', serif;">Record New Donation</h1>

        <div class="row g-4">
            
            <div class="col-md-12">
                <div class="card shadow-sm border-0">
                    <div class="card-header bg-white py-3">
                        <h5 class="mb-0 fw-bold text-muted text-uppercase small">Step 1: Find Donor</h5>
                    </div>
                    <div class="card-body p-4"> <!-- Add route -->
                        <form action="/admin/donations/add" method="GET" class="row g-3 align-items-end">
                            <div class="col-md-3"> <!--- Form -->
                                <label class="form-label fw-bold small">First Name</label>
                                <input type="text" class="form-control form-control-sm" name="firstName" value="<%= searchFirstName %>">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label fw-bold small">Last Name</label>
                                <input type="text" class="form-control form-control-sm" name="lastName" value="<%= searchLastName %>">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label fw-bold small">Username</label>
                                <input type="text" class="form-control form-control-sm" name="username" value="<%= searchUsername %>">
                            </div>
                            <div class="col-md-3 d-grid">
                                <button type="submit" class="btn btn-secondary btn-sm">Search</button>
                            </div>
                        </form>
 <!-- Loop for participatns -->
                        <% if (participants.length > 0) { %>
                            <div class="table-responsive mt-4">
                                <table class="table table-sm table-hover mb-0">
                                    <thead class="table-light">
                                        <tr> <!-- Table -->
                                            <th>Name</th>
                                            <th>Email</th>
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% participants.forEach(p => { %>
                                            <tr>
                                                <td><%= p.participantfirstname %> <%= p.participantlastname %></td>
                                                <td class="text-muted small"><%= p.participantemail %></td>
                                                <td class="text-end">
                                                    <a href="/admin/donations/add?selectedId=<%= p.participantid %>" class="btn btn-sm btn-outline-primary py-0">
                                                        Select <!-- Pull id -->
                                                    </a> 
                                                </td>
                                            </tr>
                                        <% }) %>
                                    </tbody>
                                </table>
                            </div> <!-- Check for fields -->
                        <% } else if (searchFirstName || searchLastName || searchUsername) { %>
                            <div class="alert alert-warning mt-3 mb-0 small">No participants found.</div>
                        <% } %>
                    </div>
                </div>
            </div>

            <div class="col-md-12">
                <div class="card shadow border-0 <%= selectedParticipant ? 'border-primary' : '' %>">
                    <div class="card-header <%= selectedParticipant ? 'bg-primary text-white' : 'bg-light text-muted' %> py-3">
                        <h5 class="mb-0 fw-bold">Step 2: Donation Details</h5>
                    </div>
                    <div class="card-body p-4">
                        
                        <% if (!selectedParticipant) { %>
                            <div class="text-center py-4 text-muted">
                                <i class="bi bi-arrow-up-circle display-4"></i>
                                <p class="mt-3">Please search for and <strong>select a donor</strong> above to continue.</p>
                            </div> <!-- Search For donor -->
                        <% } else { %>
                            
                            <div class="alert alert-info d-flex justify-content-between align-items-center">
                                <div>
                                    <strong>Selected Donor:</strong> <%= selectedParticipant.participantfirstname %> <%= selectedParticipant.participantlastname %>
                                    <br><small><%= selectedParticipant.participantemail %></small>
                                </div>
                                <a href="/admin/donations/add" class="btn btn-sm btn-outline-dark">Change</a>
                            </div> <!-- Add donation-->

                            <form action="/admin/donations/add" method="POST"> <!-- Add route -->
                                <input type="hidden" name="participantId" value="<%= selectedParticipant.participantid %>">

                                <div class="row mb-3"> <!-- Form -->
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold">Amount ($)</label>
                                        <input type="number" class="form-control" name="amount" min="0.01" step="0.01" required>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold">Date</label>
                                        <input type="date" class="form-control" name="date" required>
                                    </div>
                                </div>

                                <div class="mb-4">
                                    <label class="form-label fw-bold">Notes</label>
                                    <textarea class="form-control" name="notes" rows="2" placeholder="e.g. Check #123"></textarea>
                                </div>

                                <div class="d-flex justify-content-between"> <!-- Donatiojns route -->
                                    <a href="/admin/donations" class="btn btn-outline-secondary">Cancel</a>
                                    <button type="submit" class="btn btn-primary px-4">Save Donation</button>
                                </div>
                            </form>

                        <% } %>

                    </div>
                </div>
            </div>

        </div>
    </div>
 <!-- Footer -->
    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>