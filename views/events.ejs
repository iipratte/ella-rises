<!DOCTYPE html>
<html lang="en">
<head>
    <title>Events - Ella Rises</title>
    <%- include('partials/head') %> 
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5 mb-5">
        
        <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-3">
            <div>
                <h1 class="text-primary fw-bold" style="font-family: 'DM Serif Display', serif;">Upcoming Events</h1>
                <p class="text-muted mb-0">Join us for workshops, mentoring, and community service.</p>
            </div>

            <% if (user && user.role === 'Manager') { %>
                <a href="/events/add" class="btn btn-primary shadow-sm rounded-pill px-4">
                    <i class="bi bi-plus-lg me-1"></i> Add Event
                </a>
            <% } %>
        </div>

        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            
            <% if (!events || events.length === 0) { %>
                <div class="col-12 text-center py-5">
                    <div class="text-muted opacity-50 mb-3">
                        <i class="bi bi-calendar-x display-1"></i>
                    </div>
                    <h3>No events scheduled yet.</h3>
                    <p>Check back later for upcoming programs.</p>
                </div>
            <% } else { %>
                
                <% events.forEach(event => { %>
                    <div class="col">
                        <div class="card h-100 shadow-sm border-0 event-card">
                            
                            <div class="card-header border-0 py-3 d-flex justify-content-between align-items-center 
                                <%= event.eventtype === 'Leadership' ? 'bg-primary text-white' : 'bg-warning text-dark' %>">
                                <small class="fw-bold text-uppercase ls-1"><%= event.eventtype %></small>
                                <i class="bi <%= event.eventtype === 'Leadership' ? 'bi-people-fill' : 'bi-palette-fill' %>"></i>
                            </div>

                            <div class="card-body">
                                <h4 class="card-title fw-bold mt-2 mb-3"><%= event.eventname %></h4>
                                
                                <div class="mb-3 text-secondary small">
                                    <div class="d-flex align-items-center mb-2">
                                        <i class="bi bi-calendar-event me-2 text-primary"></i>
                                        <strong>
                                            <%= new Date(event.eventdatetimestart).toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric', hour: '2-digit', minute:'2-digit' }) %>
                                        </strong>
                                    </div>
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-geo-alt-fill me-2 text-primary"></i>
                                        <%= event.eventlocation %>
                                    </div>
                                </div>

                                <p class="card-text text-muted" style="font-size: 0.95rem;">
                                    <%= event.eventdescription %>
                                </p>
                            </div>

                            <div class="card-footer bg-white border-top-0 pb-4 pt-0">
                                <div class="d-flex justify-content-between align-items-center">
                                    <span class="badge bg-light text-dark border">Active</span>
                                    
                                    <% if (user && user.role === 'Manager') { %>
                                        <div class="btn-group">
                                            <a href="/events/edit/<%= event.scheduleid %>" class="btn btn-sm btn-outline-secondary">Edit</a>
                                            <form action="/events/delete/<%= event.scheduleid %>" method="POST" class="d-inline" onsubmit="return confirm('Delete this event?');">
                                                <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                                            </form>
                                        </div>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } %>

        </div>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>