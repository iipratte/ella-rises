<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - Ella Rises</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5 mb-5" style="max-width: 800px;">
        <div class="card shadow-lg border-0">
            
            <div class="card-header bg-primary text-white py-4 text-center">
                <h2 class="mb-1 fw-bold" style="font-family: 'DM Serif Display', serif;">Join the Community</h2>
                <p class="mb-0 opacity-75">Create an account to participate in events and workshops.</p>
            </div>
            
            <div class="card-body p-4 p-md-5">

                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger text-center"><%= error %></div>
                <% } %>

                <form action="/signup" method="POST">
                    
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="firstName" class="form-label fw-bold small text-uppercase text-muted">First Name</label>
                            <input 
                                id="firstName" 
                                name="firstName" 
                                type="text" 
                                class="form-control" 
                                required 
                                autocomplete="given-name"
                                value="<%= typeof firstName !== 'undefined' ? firstName : '' %>"
                            >
                        </div>
                        <div class="col-md-6">
                            <label for="lastName" class="form-label fw-bold small text-uppercase text-muted">Last Name</label>
                            <input 
                                id="lastName" 
                                name="lastName" 
                                type="text" 
                                class="form-control" 
                                required 
                                autocomplete="family-name"
                                value="<%= typeof lastName !== 'undefined' ? lastName : '' %>"
                            >
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="email" class="form-label fw-bold small text-uppercase text-muted">Email Address</label>
                            <input 
                                id="email" 
                                name="email" 
                                type="email" 
                                class="form-control" 
                                required 
                                autocomplete="email"
                                value="<%= typeof email !== 'undefined' ? email : '' %>"
                            >
                        </div>
                        <div class="col-md-6">
                            <label for="phone" class="form-label fw-bold small text-uppercase text-muted">Phone Number</label>
                            <input 
                                id="phone" 
                                name="phone" 
                                type="tel" 
                                class="form-control" 
                                required 
                                autocomplete="tel"
                                value="<%= typeof phone !== 'undefined' ? phone : '' %>"
                            >
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="dob" class="form-label fw-bold small text-uppercase text-muted">Date of Birth</label>
                        <input 
                            id="dob" 
                            name="dob" 
                            type="date" 
                            class="form-control" 
                            required 
                            value="<%= typeof dob !== 'undefined' ? dob : '' %>"
                        >
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-5">
                            <label for="city" class="form-label fw-bold small text-uppercase text-muted">City</label>
                            <input 
                                id="city" 
                                name="city" 
                                type="text" 
                                class="form-control" 
                                required 
                                autocomplete="address-level2"
                                value="<%= typeof city !== 'undefined' ? city : '' %>"
                            >
                        </div>
                        <div class="col-md-4">
                            <label for="state" class="form-label fw-bold small text-uppercase text-muted">State</label>
                            <input 
                                id="state" 
                                name="state" 
                                type="text" 
                                class="form-control" 
                                required 
                                autocomplete="address-level1"
                                value="<%= typeof state !== 'undefined' ? state : '' %>"
                            >
                        </div>
                        <div class="col-md-3">
                            <label for="zipcode" class="form-label fw-bold small text-uppercase text-muted">Zip Code</label>
                            <input 
                                id="zipcode" 
                                name="zipcode" 
                                type="number" 
                                class="form-control" 
                                required 
                                autocomplete="postal-code"
                                value="<%= typeof zipcode !== 'undefined' ? zipcode : '' %>"
                            >
                        </div>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary btn-lg">Create Account</button>
                    </div>

                    <div class="text-center mt-4">
                        <p class="text-muted small">Already have an account? <a href="/login" class="text-primary fw-bold text-decoration-none">Log in here</a></p>
                    </div>

                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>