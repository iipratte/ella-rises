<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Take Survey - Ella Rises</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5 mb-5" style="max-width: 800px;">
        <div class="card shadow-lg border-0">
            <div class="card-header bg-primary text-white py-4">
                <h2 class="mb-0 fw-bold" style="font-family: 'DM Serif Display', serif;">Program Feedback Survey</h2>
                <p class="mb-0 opacity-75">Your feedback helps us improve Ella Rises events.</p>
            </div>
            
            <div class="card-body p-5">
                
                <% if (user && user.role === 'Manager') { %>
                    <div class="alert alert-info mb-4">
                        <i class="bi bi-info-circle-fill me-2"></i> Logged in as <strong>Admin/Manager</strong>
                    </div>
                <% } else if (user) { %>
                    <div class="alert alert-success mb-4">
                        <i class="bi bi-check-circle-fill me-2"></i> Logged in as <strong>Participant</strong>
                    </div>
                <% } %>

                <form action="/surveys/submit" method="POST">
                    
                    <h5 class="text-primary fw-bold text-uppercase mb-3">Participant Information</h5>
                    
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="firstName" class="form-label">First Name</label>
                            <input type="text" class="form-control" id="firstName" name="firstName" required>
                        </div>
                        <div class="col-md-6">
                            <label for="lastName" class="form-label">Last Name</label>
                            <input type="text" class="form-control" id="lastName" name="lastName" required>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" class="form-control" id="email" name="email" required>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-6">
                            <label for="schoolEmployer" class="form-label">School or Employer</label>
                            <input type="text" class="form-control" id="schoolEmployer" name="schoolEmployer">
                        </div>
                        <div class="col-md-6">
                            <label for="programArea" class="form-label">Program Area</label>
                            <select class="form-select" id="programArea" name="programArea" required>
                                <option value="STEM" selected>STEM</option>
                                <option value="arts">Arts</option>
                                <option value="both">Both</option>
                            </select>
                        </div>
                    </div>

                    <hr class="my-4">

                    <h5 class="text-primary fw-bold text-uppercase mb-3">Event Details</h5>
                    
                    <div class="mb-3">
                        <label for="eventName" class="form-label fw-bold">Which event did you attend?</label>
                        <select class="form-select" id="eventName" name="eventName" required>
                            <option value="" selected disabled>-- Select an Event --</option>
                            
                            <% if (events && events.length > 0) { %>
                                <% events.forEach(e => { %>
                                    <option value="<%= e.eventname %>"><%= e.eventname %></option>
                                <% }) %>
                            <% } else { %>
                                <option value="General Program">General Program</option>
                            <% } %>
                        </select>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-6">
                            <label for="eventType" class="form-label">Event Type</label>
                            <select class="form-select" id="eventType" name="eventType" required>
                                <option value="Leadership">Leadership</option>
                                <option value="STEM">STEM</option>
                                <option value="Arts">Arts</option>
                                <option value="Annual Conference">Annual Conference</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label for="occurrence" class="form-label">Frequency</label>
                            <select class="form-select" id="occurrence" name="occurrence" required>
                                <option value="weekly">Weekly</option>
                                <option value="monthly">Monthly</option>
                                <option value="annually">Annually</option>
                            </select>
                        </div>
                    </div>

                    <hr class="my-4">

                    <h5 class="text-primary fw-bold text-uppercase mb-3">Feedback & Ratings</h5>
                    <p class="text-muted small">Please rate on a scale of 1 (Low) to 5 (High).</p>

                    <div class="mb-4 bg-light p-3 rounded">
                        <label class="form-label fw-bold d-block">How would you rate your satisfaction with this event?</label>
                        <div class="btn-group w-100" role="group">
                            <% for (let i = 1; i <= 5; i++) { %>
                                <input type="radio" class="btn-check" name="satisfaction" id="sat<%= i %>" value="<%= i %>" required>
                                <label class="btn btn-outline-primary" for="sat<%= i %>"><%= i %></label>
                            <% } %>
                        </div>
                    </div>

                    <div class="mb-4 bg-light p-3 rounded">
                        <label class="form-label fw-bold d-block">How useful was this event for your goals?</label>
                        <div class="btn-group w-100" role="group">
                            <% for (let i = 1; i <= 5; i++) { %>
                                <input type="radio" class="btn-check" name="usefulness" id="use<%= i %>" value="<%= i %>" required>
                                <label class="btn btn-outline-primary" for="use<%= i %>"><%= i %></label>
                            <% } %>
                        </div>
                    </div>

                    <div class="mb-4 bg-light p-3 rounded">
                        <label class="form-label fw-bold d-block">How would you rate the instructor?</label>
                        <div class="btn-group w-100" role="group">
                            <% for (let i = 1; i <= 5; i++) { %>
                                <input type="radio" class="btn-check" name="instructorScore" id="inst<%= i %>" value="<%= i %>" required>
                                <label class="btn btn-outline-primary" for="inst<%= i %>"><%= i %></label>
                            <% } %>
                        </div>
                    </div>

                    <div class="mb-4 bg-light p-3 rounded">
                        <label class="form-label fw-bold d-block">Would you recommend this event to others?</label>
                        <div class="btn-group w-100" role="group">
                            <% for (let i = 1; i <= 5; i++) { %>
                                <input type="radio" class="btn-check" name="recommendationScore" id="rec<%= i %>" value="<%= i %>" required>
                                <label class="btn btn-outline-primary" for="rec<%= i %>"><%= i %></label>
                            <% } %>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label for="comments" class="form-label fw-bold">Additional Comments</label>
                        <textarea class="form-control" id="comments" name="comments" rows="4" maxlength="250" placeholder="Share your thoughts..."></textarea>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg">Submit Survey</button>
                    </div>

                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>