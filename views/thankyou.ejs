<!DOCTYPE html>
<html lang="en">
<head>
    <title>Thank You</title>
    <%- include('partials/head') %>
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5 text-center">
        <div class="card shadow-lg p-5">
            <h1 class="text-success"><i class="bi bi-check-circle-fill"></i> Thank You!</h1>
            
            <% if (typeof type !== 'undefined' && type === 'survey') { %>
                <h2 class="mt-3">Your Feedback Has Been Received.</h2>
                <p class="lead text-muted">We appreciate you taking the time to help us improve our programs.</p>
            <% } else { %>
                <h2 class="mt-3">Your Donation Has Been Recorded.</h2>
                <p class="lead text-muted">Your support directly impacts the next generation of women in STEAM and leadership.</p>
            <% } %>

            <a href="/" class="btn btn-primary mt-4" style="width: 200px; margin: 0 auto;">Return Home</a>
        </div>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>