<!DOCTYPE html>
<html lang="en">
<head>
    <title>Add Participant - Ella Rises</title>
    <%- include('partials/head') %> 
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5" style="max-width: 600px;">
        <div class="card shadow border-0">
            <div class="card-header bg-primary text-white py-3">
                <h4 class="mb-0 fw-bold" style="font-family: 'DM Serif Display', serif;">Add New Participant</h4>
            </div>
            <div class="card-body p-4">
                
                <form action="/participants/add" method="POST">
                    
                    <div class="mb-3">
                        <label for="username" class="form-label fw-bold">Username / Name</label>
                        <input type="text" class="form-control" id="username" name="username" required placeholder="Enter participant username or name">
                    </div>

                    <div class="mb-3">
                        <label for="schoolOrEmployer" class="form-label fw-bold">School or Employer</label>
                        <input type="text" class="form-control" id="schoolOrEmployer" name="schoolOrEmployer" placeholder="e.g. UVU or Local Business">
                    </div>

                    <div class="mb-4">
                        <label for="fieldOfInterest" class="form-label fw-bold">Field of Interest</label>
                        <input type="text" class="form-control" id="fieldOfInterest" name="fieldOfInterest" placeholder="e.g. Computer Science, Art, Nursing">
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="/participants" class="btn btn-outline-secondary">Cancel</a>
                        <button type="submit" class="btn btn-primary px-4">Save Participant</button>
                    </div>

                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>