<!DOCTYPE html>
<html lang="en">
<head>
    <title>Edit Survey - Ella Rises</title>
    <%- include('partials/head') %>
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5 mb-5" style="max-width: 600px;">
        <div class="card shadow-lg border-0">
            <div class="card-header py-3" style="background-color: var(--er-primary);">
                <h4 class="mb-0 fw-bold text-white">Edit Survey Response</h4>
            </div>
            
            <div class="card-body p-4">
                <div class="alert alert-light border mb-4">
                    <strong>Event:</strong> <%= survey.eventname %><br>
                    <strong>Participant:</strong> <%= survey.participantfirstname %> <%= survey.participantlastname %>
                </div>

                <form action="/survey/edit/<%= survey.surveyid %>" method="POST">
                    
                    <div class="row mb-3">
                        <div class="col-4">
                            <label class="form-label fw-bold">Satisfaction (1-5)</label>
                            <input type="number" class="form-control" name="satisfaction" min="1" max="5" required value="<%= survey.surveysatisfactionscore %>">
                        </div>
                        <div class="col-4">
                            <label class="form-label fw-bold">Usefulness (1-5)</label>
                            <input type="number" class="form-control" name="usefulness" min="1" max="5" required value="<%= survey.surveyusefulnessscore %>">
                        </div>
                        <div class="col-4">
                            <label class="form-label fw-bold">Rec (1-5)</label>
                            <input type="number" class="form-control" name="recommendation" min="1" max="5" required value="<%= survey.surveyrecommendationscore %>">
                        </div>
                    </div>

                    <div class="mb-4">
                        <label class="form-label fw-bold">Comments</label>
                        <textarea class="form-control" name="comments" rows="4"><%= survey.surveycomments %></textarea>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="/admin/survey-data" class="btn btn-outline-secondary">Cancel</a>
                        <button type="submit" class="btn btn-primary px-4">Save Changes</button>
                    </div>

                </form>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>