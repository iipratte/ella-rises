<!DOCTYPE html>
<html lang="en">
<head>
    <title>Participant History - Ella Rises</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h6 class="text-uppercase text-muted mb-1">Milestone History For</h6>
                <h1 class="fw-bold text-primary display-6">
                    <%= participant.participantfirstname %> <%= participant.participantlastname %>
                </h1>
            </div>
            <a href="/milestones" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Back to List
            </a>
        </div>

        <div class="card shadow border-0">
            <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center">
                <h5 class="mb-0 fw-bold">Achievements Timeline</h5>
                <a href="/milestones/add/<%= participant.participantid %>" class="btn btn-success btn-sm">
                    <i class="bi bi-plus-lg"></i> Add New
                </a>
            </div>

            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-striped mb-0">
                        <thead class="table-light">
                            <tr>
                                <th class="ps-4">Date</th>
                                <th>Milestone Type</th>
                                <th>Notes</th>
                                <th class="text-end pe-4">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (milestones.length === 0) { %>
                                <tr>
                                    <td colspan="4" class="text-center py-5 text-muted">
                                        No milestones recorded yet.
                                    </td>
                                </tr>
                            <% } else { %>
                                <% milestones.forEach(m => { %>
                                    <tr>
                                        <td class="ps-4 text-nowrap">
                                            <%= new Date(m.dateachieved).toLocaleDateString() %>
                                        </td>
                                        <td class="fw-bold text-primary"><%= m.milestonename %></td>
                                        <td><%= m.milestonenotes || '-' %></td>
                                        <td class="text-end pe-4">
                                            <form action="/milestones/delete/<%= m.milestoneid %>" method="POST" onsubmit="return confirm('Delete this milestone?');">
                                                <button type="submit" class="btn btn-sm btn-outline-danger border-0">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</body>
</html>