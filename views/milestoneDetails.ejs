<!DOCTYPE html>
<html lang="en">
<head>
    <title>Milestones - <%= participant.participantfirstname %> <%= participant.participantlastname %></title>
    <%- include('partials/head') %>
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5 mb-5">
        
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb mb-1">
                        <li class="breadcrumb-item"><a href="/participants">Participants</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Milestones</li>
                    </ol>
                </nav>
                <h1 class="text-primary fw-bold" style="font-family: 'DM Serif Display', serif;">
                    <%= participant.participantfirstname %>'s Milestones
                </h1>
                <p class="text-muted">
                    Tracking achievements for <span class="fw-bold"><%= participant.participantfirstname %> <%= participant.participantlastname %></span>.
                </p>
            </div>
            
            <div class="d-flex gap-2">
                <a href="/participants" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Back
                </a>
                <a href="/milestones/add/<%= participant.participantid %>" class="btn btn-success shadow-sm">
                    <i class="bi bi-plus-lg"></i> Add Milestone
                </a>
            </div>
        </div>

        <div class="row">
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm border-0">
                    <div class="card-body text-center py-4">
                        <div class="mb-3">
                            <div class="rounded-circle bg-primary bg-opacity-10 d-inline-flex align-items-center justify-content-center text-primary fw-bold fs-3" style="width: 80px; height: 80px;">
                                <%= participant.participantfirstname.charAt(0) %><%= participant.participantlastname.charAt(0) %>
                            </div>
                        </div>
                        <h5 class="card-title fw-bold"><%= participant.participantfirstname %> <%= participant.participantlastname %></h5>
                        <p class="card-text text-muted small">
                            <%= participant.participantschooloremployer || 'No School/Employer Listed' %><br>
                            <%= participant.participantemail %>
                        </p>
                        
                        <hr class="my-3">
                        
                        <div class="d-flex justify-content-between px-3">
                            <span class="text-muted small">Total Milestones</span>
                            <span class="fw-bold text-primary"><%= milestones.length %></span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-md-8">
                
                <% if (milestones.length === 0) { %>
                    <div class="card shadow-sm border-0 text-center py-5">
                        <div class="card-body">
                            <div class="text-muted mb-3">
                                <i class="bi bi-trophy display-4 opacity-25"></i>
                            </div>
                            <h5 class="text-muted">No milestones recorded yet.</h5>
                            <p class="text-muted small mb-4">Start tracking achievements by adding a milestone.</p>
                            <a href="/milestones/add/<%= participant.participantid %>" class="btn btn-sm btn-primary">
                                Add First Milestone
                            </a>
                        </div>
                    </div>
                <% } else { %>
                    
                    <div class="vstack gap-3">
                        <% milestones.forEach(m => { %>
                            <div class="card shadow-sm border-0 border-start border-4 border-success">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-start mb-2">
                                        <div>
                                            <h5 class="card-title fw-bold text-dark mb-1">
                                                <%= m.milestonetitle %>
                                            </h5>
                                            <div class="text-muted small">
                                                <i class="bi bi-calendar-event me-1"></i>
                                                <% 
                                                    // Simple date formatting
                                                    const dateObj = new Date(m.milestonedate);
                                                    const dateStr = dateObj.toLocaleDateString('en-US', { 
                                                        year: 'numeric', month: 'long', day: 'numeric' 
                                                    });
                                                %>
                                                <%= dateStr %>
                                            </div>
                                        </div>
                                        
                                        <div class="dropdown">
                                            <button class="btn btn-sm btn-outline-secondary" type="button" data-bs-toggle="dropdown">
                                                <i class="bi bi-three-dots-vertical"></i>
                                            </button>
                                            <ul class="dropdown-menu dropdown-menu-end shadow">
                                                <li>
                                                    <a class="dropdown-item" href="/milestones/edit/<%= m.milestoneid %>">
                                                        <i class="bi bi-pencil me-2 text-secondary"></i> Edit
                                                        
                                                    </a>
                                                </li>
                                                <li><hr class="dropdown-divider"></li>
                                                <li>
                                                    <form action="/milestones/delete/<%= m.milestoneid %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this milestone?');">
                                                        <button type="submit" class="dropdown-item text-danger">
                                                            <i class="bi bi-trash me-2"></i> Delete
                                                        </button>
                                                    </form>
                                                </li>
                                            </ul>
                                        </div>
                                    </div>
                                    
                                    <% if (m.milestonenotes) { %>
                                        <p class="card-text text-secondary mt-3">
                                            <%= m.milestonenotes %>
                                        </p>
                                    <% } %>
                                </div>
                            </div>
                        <% }) %>
                    </div>

                <% } %>

            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>