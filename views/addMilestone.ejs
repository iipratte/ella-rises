<!DOCTYPE html>
<html lang="en">
<head>
    <title>Add Milestone - Ella Rises</title>
    <%- include('partials/head') %>
</head>
<body class="bg-light">
        <!-- Header -->
    <%- include('partials/header') %>

    <div class="container mt-5" style="max-width: 600px;">
        <div class="card shadow border-0">
            <div class="card-header bg-primary text-white py-3"> <!--Adding a milestone-->
                <h5 class="mb-0 fw-bold" style="font-family: 'DM Serif Display', serif;">Add New Milestone</h5>
                <small class="text-white-50">Adding for <%= participant.participantfirstname %> <%= participant.participantlastname %></small>
            </div>
            <div class="card-body p-4">
                <!--Add route and form-->
                <form action="/milestones/add/<%= participant.participantid %>" method="POST">
                    
                    <div class="mb-3">
                        <label for="milestoneTitle" class="form-label fw-bold">Milestone Title</label>
                        <input type="text" class="form-control" id="milestoneTitle" name="milestoneTitle" 
                               placeholder="e.g. High School Diploma, Internship, Competition Award" required>
                    </div>

                    <div class="mb-3">
                        <label for="milestoneDate" class="form-label fw-bold">Date Achieved</label>
                        <input type="date" class="form-control" id="milestoneDate" name="milestoneDate" required>
                    </div>

                    <div class="mb-4">
                        <label for="notes" class="form-label fw-bold">Notes</label>
                        <textarea class="form-control" id="notes" name="notes" rows="3" placeholder="Optional details..."></textarea>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="/milestones" class="btn btn-outline-secondary">Cancel</a>
                        <button type="submit" class="btn btn-primary px-4">Save Milestone</button>
                    </div>

                </form>
            </div>
        </div>
    </div>
  <!--  Footer -->
    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>