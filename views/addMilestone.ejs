<!DOCTYPE html>
<html lang="en">
<head>
    <title>Add Milestone - Ella Rises</title>
    <%- include('partials/head') %> 
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5" style="max-width: 600px;">
        <div class="card shadow-lg border-0">
            <div class="card-header bg-success text-white py-3">
                <h4 class="mb-0 fw-bold">Record Achievement</h4>
                <p class="mb-0 small opacity-75">
                    For: <%= participant.participantfirstname %> <%= participant.participantlastname %>
                </p>
            </div>
            
            <div class="card-body p-4">
                <form action="/milestones/add/<%= participant.participantid %>" method="POST">
                    
                    <div class="mb-3">
                        <label class="form-label fw-bold">Milestone Type</label>
                        <select class="form-select" name="milestoneName" required>
                            <option value="High School Graduation">High School Graduation</option>
                            <option value="College Acceptance">College Acceptance</option>
                            <option value="Scholarship Received">Scholarship Received</option>
                            <option value="Internship Secured">Internship Secured</option>
                            <option value="Mission Call">Mission Call</option>
                            <option value="Community Award">Community Award</option>
                            <option value="Other">Other</option>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Date Achieved</label>
                        <input type="date" class="form-control" name="dateAchieved" required>
                    </div>

                    <div class="mb-4">
                        <label class="form-label fw-bold">Notes / Details</label>
                        <textarea class="form-control" name="notes" rows="3" placeholder="e.g., Accepted to BYU Nursing Program"></textarea>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="/milestones/view/<%= participant.participantid %>" class="btn btn-outline-secondary">Cancel</a>
                        <button type="submit" class="btn btn-success px-4">Save Milestone</button>
                    </div>

                </form>
            </div>
        </div>
    </div>
</body>
</html>