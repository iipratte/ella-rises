<!DOCTYPE html>
<html lang="en">
<head> <!-- Title -->
    <title>Donation History - Ella Rises</title>
    <%- include('../partials/head') %> 
</head>
<body class="bg-light">
 <!-- HEader -->
    <%- include('../partials/header') %>

    <div class="container mt-5 mb-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="fw-bold text-primary" style="font-family: 'DM Serif Display', serif;">Donation History</h1>
                <p class="text-muted">Track all contributions to the organization.</p>
            </div>
            
            <div class="d-flex gap-3">
                <div class="bg-white px-4 py-2 rounded shadow-sm border text-center">
                    <small class="text-muted text-uppercase fw-bold" style="font-size: 0.7rem;">Total Raised</small>
                    <h4 class="text-success fw-bold mb-0">
                        $<%= donations.reduce((sum, d) => sum + Number(d.donationamount), 0).toLocaleString('en-US', {minimumFractionDigits: 2}) %>
                    </h4> <!-- Total donations -->
                </div>
                
                <a href="/admin/donations/add" class="btn btn-primary shadow-sm d-flex align-items-center">
                    <i class="bi bi-plus-lg me-2"></i> Record Donation
                </a> <!-- Add donation route -->
            </div>
        </div>

        <div class="card shadow border-0">
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover table-striped mb-0 align-middle">
                        <thead class="table-dark">
                            <tr> <!-- Table -->
                                <th class="ps-4">Date</th>
                                <th>Donor Name</th>
                                <th>Email</th>
                                <th>Notes</th>
                                <th class="text-end">Amount</th>
                                <th class="text-end pe-4">Actions</th>
                            </tr>
                        </thead>
                        <tbody> <!-- loop for table -->
                            <% if (donations.length === 0) { %>
                                <tr><td colspan="6" class="text-center py-5 text-muted">No donations recorded yet.</td></tr>
                            <% } else { %>
                                <% donations.forEach(d => { %>
                                    <tr>
                                        <td class="ps-4">
                                            <% if (d.donationdate) { %>
                                                <%= new Date(d.donationdate).toLocaleDateString() %>
                                            <% } else { %>
                                                <span class="text-muted small fst-italic">Unknown</span>
                                            <% } %>
                                        </td>
                                        <td class="fw-bold">
                                            <%= d.participantfirstname %> <%= d.participantlastname %>
                                        </td>
                                        <td>
                                            <a href="mailto:<%= d.participantemail %>" class="text-decoration-none text-secondary">
                                                <%= d.participantemail %>
                                            </a>
                                        </td>
                                        <td class="small text-muted text-truncate" style="max-width: 200px;">
                                            <%= d.donationnotes || '-' %>
                                        </td>
                                        <td class="text-end fw-bold text-success">
                                            $<%= Number(d.donationamount).toLocaleString('en-US', {minimumFractionDigits: 2}) %>
                                        </td>
                                        <td class="text-end pe-4">
                                            <div class="d-flex justify-content-end gap-2">
                                                <a href="/admin/donations/edit/<%= d.donationid %>" class="btn btn-sm btn-outline-secondary" title="Edit">
                                                    Edit <!-- Edit route -->
                                                </a>
                                                <form action="/admin/donations/delete/<%= d.donationid %>" method="POST" class="d-inline" onsubmit="return confirm('Delete this donation record?');">
                                                    <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                                        <i class="bi bi-trash-fill"></i> <!-- Delete route -->
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div> 
 <!-- Footer -->
    <%- include('../partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>