<!DOCTYPE html>
<html lang="en">
<head>
    <title>Donation History - Ella Rises</title>
    <%- include('../partials/head') %> 
</head>
<body class="bg-light">

    <%- include('../partials/header') %>

    <div class="container mt-5 mb-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="fw-bold text-primary" style="font-family: 'DM Serif Display', serif;">Donation History</h1>
                <p class="text-muted">Track all contributions to the organization.</p>
            </div>
            <div class="bg-white p-3 rounded shadow-sm border text-center">
                <small class="text-muted text-uppercase fw-bold">Total Donations</small>
                <h3 class="text-success fw-bold mb-0">
                    $<%= donations.reduce((sum, d) => sum + Number(d.donationamount), 0).toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %>
                </h3>
            </div>
        </div>

        <div class="card shadow border-0">
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover table-striped mb-0">
                        <thead class="table-dark">
                            <tr>
                                <th class="ps-4">Date</th>
                                <th>Donor Name</th>
                                <th>Email</th>
                                <th class="text-end pe-4">Amount</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (donations.length === 0) { %>
                                <tr><td colspan="4" class="text-center py-5">No donations recorded yet.</td></tr>
                            <% } else { %>
                                <% donations.forEach(d => { %>
                                    <tr>
                                        <td class="ps-4">
                                            <% if (d.donationdate) { %>
                                                <%= new Date(d.donationdate).toLocaleDateString() %>
                                            <% } else { %>
                                                <span class="text-muted small fst-italic">Unknown Date</span>
                                            <% } %>
                                        </td>

                                        <td class="fw-bold">
                                            <% if (d.participantfirstname) { %>
                                                <%= d.participantfirstname %> <%= d.participantlastname %>
                                            <% } else { %>
                                                <span class="text-muted fst-italic">
                                                    <i class="bi bi-person-x-fill me-1"></i> (Deleted User)
                                                </span>
                                            <% } %>
                                        </td>
                                        
                                        <td>
                                            <% if (d.participantemail) { %>
                                                <a href="mailto:<%= d.participantemail %>" class="text-decoration-none">
                                                    <%= d.participantemail %>
                                                </a>
                                            <% } else { %>
                                                <span class="text-muted small">-</span>
                                            <% } %>
                                        </td>

                                        <td class="text-end pe-4 fw-bold text-success">
                                            $<%= Number(d.donationamount).toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>