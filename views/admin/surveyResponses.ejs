<!DOCTYPE html>
<html lang="en">
<head>
    <title>Survey Responses - Ella Rises</title>
    <%- include('../partials/head') %>
</head>
<body class="bg-light">

    <%- include('../partials/header') %>

    <div class="container mt-5">
        <h1 class="text-primary fw-bold mb-4" style="font-family: 'DM Serif Display', serif;">Survey Responses</h1>

        <div class="card shadow border-0">
            <div class="card-body p-0">
                <table class="table table-hover table-striped mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th>Event Name</th>
                            <th>Satisfaction</th>
                            <th>Usefulness</th>
                            <th>Comments</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (responses.length === 0) { %>
                            <tr><td colspan="4" class="text-center py-4">No responses yet.</td></tr>
                        <% } else { %>
                            <% responses.forEach(r => { %>
                                <tr>
                                    <td class="fw-bold"><%= r.event_name %></td>
                                    <td><%= r.satisfaction_score %>/5</td>
                                    <td><%= r.usefulness_score || '-' %>/5</td> <td class="text-muted small"><%= r.comments %></td>
                                </tr>
                            <% }) %>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div><br>

    <%- include('../partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>