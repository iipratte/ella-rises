<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Surveys - Ella Rises</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5 mb-5">
        
        <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-3">
            <div>
                <h1 class="text-primary fw-bold" style="font-family: 'DM Serif Display', serif;">Surveys & Feedback</h1>
                <p class="text-muted mb-0">Help us improve by sharing your experience.</p>
            </div>

            <% if (user && user.role === 'Manager') { %>
                <button class="btn btn-primary shadow-sm rounded-pill px-4">
                    <i class="bi bi-plus-lg me-1"></i> Create Survey
                </button>
            <% } %>
        </div>

        <div class="row row-cols-1 row-cols-md-2 g-4">
            
            <% if (surveys.length === 0) { %>
                <div class="col-12 text-center py-5">
                    <p class="text-muted">No active surveys at this time.</p>
                </div>
            <% } else { %>
                
                <% surveys.forEach(survey => { %>
                    <div class="col">
                        <div class="card h-100 shadow-sm border-0 hover-effect">
                            <div class="card-body p-4">
                                <div class="d-flex justify-content-between align-items-start mb-3">
                                    <div>
                                        <span class="badge bg-secondary mb-2"><%= survey.surveytype || 'Feedback' %></span>
                                        <h4 class="card-title fw-bold"><%= survey.surveyname %></h4>
                                    </div>
                                    <i class="bi bi-clipboard-data text-muted fs-3"></i>
                                </div>
                                
                                <p class="card-text text-muted mb-4">
                                    <%= survey.surveydescription %>
                                </p>

                                <div class="d-grid">
                                    <a href="/surveys/take/<%= survey.surveyid %>" class="btn btn-outline-primary">
                                        Take Survey
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>

            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>