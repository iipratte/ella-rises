<!DOCTYPE html>
<html lang="en">
<head>
    <title>Program Feedback - Ella Rises</title>
    <%- include('partials/head') %>
    <style>
        /* --- RATING CIRCLES --- */
        .rating-group {
            display: flex;
            justify-content: center;
            gap: 35px; /* Nice spacing between numbers */
            margin-top: 10px;
            margin-bottom: 10px;
        }
        
        /* Hide the actual radio buttons */
        .rating-input { display: none; }

        /* The Circle Label */
        .rating-label {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background-color: #fff;
            border: 2px solid #e9ecef;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            color: var(--er-charcoal);
            cursor: pointer;
            transition: all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            font-size: 1.1rem;
        }

        /* Hover State */
        .rating-label:hover {
            border-color: var(--er-primary);
            color: var(--er-primary);
            transform: translateY(-3px);
        }

        /* Selected State */
        .rating-input:checked + .rating-label {
            background-color: var(--er-primary); /* Brand Pink */
            border-color: var(--er-primary);
            color: white;
            transform: scale(1.15);
            box-shadow: 0 4px 10px rgba(249, 175, 177, 0.5);
        }

        /* Card Styling */
        .feedback-card {
            border: none;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        }
        
        .section-title {
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #adb5bd;
            font-weight: 700;
            margin-bottom: 1.5rem;
            text-align: center;
            position: relative;
        }
        
        .section-title::before {
            content: "";
            position: absolute;
            left: 0; top: 50%;
            width: 100%;
            height: 1px;
            background: #f0f0f0;
            z-index: 0;
        }
        .section-title span {
            background: #fff;
            padding: 0 15px;
            position: relative;
            z-index: 1;
        }
    </style>
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-4 mb-5" style="max-width: 600px;">
        
        <div class="text-center mb-4">
            <h1 class="fw-bold text-primary" style="font-family: 'DM Serif Display', serif;">We Value Your Voice</h1>
            <p class="text-muted">Help us improve Ella Rises for future participants.</p>
        </div>

        <div class="card feedback-card">
            <div class="card-header bg-primary text-white py-3 text-center" style="background-color: var(--er-sage) !important;">
                <h5 class="mb-0 fw-bold"><i class="bi bi-chat-square-heart-fill me-2"></i> Event Feedback Survey</h5>
            </div>

            <div class="card-body p-4 p-md-5">
                
                <% if (!myParticipants || myParticipants.length === 0) { %>
                    
                    <div class="text-center py-4">
                        <div class="mb-3 text-muted">
                            <i class="bi bi-people display-1 opacity-25"></i>
                        </div>
                        <h4 class="fw-bold text-secondary">No Participants Found</h4>
                        <p class="text-muted small mb-4">
                            You must have a participant linked to your account to submit feedback.
                        </p>
                        <a href="/participants/add" class="btn btn-primary rounded-pill px-4 shadow-sm">
                            <i class="bi bi-plus-lg me-1"></i> Add Participant
                        </a>
                    </div>

                <% } else { %>

                    <form action="/survey/submit" method="POST">

                        <% if (myParticipants.length === 1) { %>
                            <input type="hidden" name="participantId" value="<%= myParticipants[0].participantid %>">
                        <% } else { %>
                            <div class="mb-4">
                                <label for="participantId" class="form-label fw-bold text-dark">Who attended?</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light border-end-0"><i class="bi bi-person"></i></span>
                                    <select class="form-select border-start-0 ps-0" id="participantId" name="participantId" required style="background-color: #f8f9fa;">
                                        <option value="" selected disabled>-- Select Child --</option>
                                        <% myParticipants.forEach(p => { %>
                                            <option value="<%= p.participantid %>">
                                                <%= p.participantfirstname %> <%= p.participantlastname %>
                                            </option>
                                        <% }) %>
                                    </select>
                                </div>
                            </div>
                        <% } %>

                        <div class="mb-5">
                            <label for="scheduleId" class="form-label fw-bold text-dark">Which event are you reviewing?</label>
                            
                            <div class="input-group mb-2">
                                <span class="input-group-text bg-white border-end-0"><i class="bi bi-search text-muted"></i></span>
                                <input type="text" class="form-control border-start-0" id="eventSearch" placeholder="Type to filter events..." onkeyup="filterEvents()">
                            </div>

                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0"><i class="bi bi-calendar-event"></i></span>
                                <select class="form-select border-start-0 ps-0" id="scheduleId" name="scheduleId" required style="background-color: #f8f9fa;" size="5">
                                    <option value="" selected disabled>-- Select an Event --</option>
                                    <% if (pastEvents && pastEvents.length > 0) { %>
                                        <% pastEvents.forEach(e => { %>
                                            <option value="<%= e.scheduleid %>">
                                                <%= e.eventname %> (<%= new Date(e.eventdatetimestart).toLocaleDateString() %>)
                                            </option>
                                        <% }) %>
                                    <% } else { %>
                                        <option disabled>No past events found.</option>
                                    <% } %>
                                </select>
                            </div>
                            <div class="form-text small">Select the event from the list above.</div>
                        </div>

                        <div class="section-title"><span>Experience Ratings</span></div>

                        <div class="mb-5 text-center">
                            <label class="form-label fw-bold d-block mb-2">Overall Satisfaction</label>
                            <div class="d-flex justify-content-between px-3 small text-muted text-uppercase fw-bold mb-1">
                                <span>Poor</span>
                                <span>Excellent</span>
                            </div>
                            <div class="rating-group">
                                <% for(let i=1; i<=5; i++) { %>
                                    <input type="radio" class="rating-input" name="satisfaction" id="sat<%= i %>" value="<%= i %>" required>
                                    <label class="rating-label" for="sat<%= i %>"><%= i %></label>
                                <% } %>
                            </div>
                        </div>

                        <div class="row mb-4">
                            <div class="col-md-12 text-center mb-4">
                                <label class="form-label fw-bold d-block text-secondary small">Content Usefulness</label>
                                <div class="rating-group justify-content-center">
                                    <% for(let i=1; i<=5; i++) { %>
                                        <input type="radio" class="rating-input" name="usefulness" id="use<%= i %>" value="<%= i %>" required>
                                        <label class="rating-label" style="width:35px; height:35px; font-size: 0.9rem;" for="use<%= i %>"><%= i %></label>
                                    <% } %>
                                </div>
                            </div>
                        </div>

                        <div class="section-title mt-5"><span>Recommendation</span></div>

                        <div class="mb-5 text-center">
                            <label class="form-label fw-bold d-block">How likely are you to recommend us?</label>
                            <div class="d-flex justify-content-between px-3 small text-muted text-uppercase fw-bold mb-1">
                                <span>Not Likely</span>
                                <span>Very Likely</span>
                            </div>
                            <div class="rating-group">
                                <% for(let i=1; i<=5; i++) { %>
                                    <input type="radio" class="rating-input" name="recommendation" id="rec<%= i %>" value="<%= i %>" required>
                                    <label class="rating-label" for="rec<%= i %>"><%= i %></label>
                                <% } %>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label for="comments" class="form-label fw-bold">Additional Comments</label>
                            <textarea class="form-control bg-light" id="comments" name="comments" rows="4" placeholder="What did you enjoy? What can we improve?"></textarea>
                        </div>

                        <div class="d-grid mt-5">
                            <button type="submit" class="btn btn-primary btn-lg shadow-sm">Submit Feedback</button>
                        </div>

                    </form>
                <% } %> </div>
        </div>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        function filterEvents() {
            const input = document.getElementById('eventSearch');
            if (!input) return; // Guard clause if element missing
            
            const filter = input.value.toLowerCase();
            const select = document.getElementById('scheduleId');
            const options = select.getElementsByTagName('option');

            for (let i = 0; i < options.length; i++) {
                const txtValue = options[i].textContent || options[i].innerText;
                if (txtValue.toLowerCase().indexOf(filter) > -1) {
                    options[i].style.display = "";
                } else {
                    if (options[i].value !== "") { // Don't hide the placeholder
                        options[i].style.display = "none";
                    }
                }
            }
        }
    </script>
</body>
</html>