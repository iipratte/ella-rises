<!DOCTYPE html>
<html lang="en">
<head>
    <title>Program Feedback - Ella Rises</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="/styles.css">
    
    <style>
        /* Compact Rating Circles */
        .rating-group {
            display: flex;
            justify-content: center;
            gap: 65px; 
            margin-top: 10px;
        }
        
        .rating-input {
            display: none;
        }

        .rating-label {
            width: 42px;
            height: 42px;
            border-radius: 50%;
            background-color: #fff;
            border: 2px solid #dee2e6;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            font-size: 0.9rem;
            color: #6c757d;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        /* Hover & Checked States */
        .rating-input:checked + .rating-label,
        .rating-label:hover {
            background-color: var(--er-primary);
            border-color: var(--er-primary);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(194, 24, 91, 0.2);
        }
    </style>
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-4 mb-5" style="max-width: 600px;">
        
        <div class="card shadow border-0">
            
            <div class="card-header text-center py-4" style="background-color: var(--er-primary); border-bottom: none;">
                <h1 class="h2 fw-bold text-white mb-1" style="font-family: 'Playfair Display', serif;">Program Feedback</h1>
                <p class="text-white-50 mb-0 small">We value your voice. Help us improve.</p>
            </div>
            
            <div class="card-body p-4">
                <form action="/survey/submit" method="POST">
                    
                    <div class="mb-4">
                        <label for="eventName" class="form-label fw-bold text-uppercase text-secondary" style="font-size: 0.75rem;">Which event did you attend?</label>
                        <select class="form-select" id="eventName" name="eventName" required>
                            <option value="" selected disabled>-- Select an Event --</option>
                            <% if (events && events.length > 0) { %>
                                <% events.forEach(e => { %>
                                    <option value="<%= e.eventname %>"><%= e.eventname %></option>
                                <% }) %>
                            <% } else { %>
                                <option value="General Program">General Program</option>
                            <% } %>
                        </select>
                    </div>

                    <hr class="my-4 opacity-10">

                    <div class="mb-4 text-center">
                        <label class="form-label fw-bold d-block mb-1">Overall Satisfaction</label>
                        <div class="d-flex justify-content-between small text-muted text-uppercase fw-bold px-3" style="font-size: 0.7rem;">
                            <span>Poor</span>
                            <span>Excellent</span>
                        </div>
                        
                        <div class="rating-group">
                            <% for (let i = 1; i <= 5; i++) { %>
                                <div>
                                    <input type="radio" class="rating-input" name="satisfaction" id="sat<%= i %>" value="<%= i %>" required>
                                    <label class="rating-label" for="sat<%= i %>"><%= i %></label>
                                </div>
                            <% } %>
                        </div>
                    </div>

                    <div class="mb-4 text-center">
                        <label class="form-label fw-bold d-block mb-1">Content Usefulness</label>
                        <div class="d-flex justify-content-between small text-muted text-uppercase fw-bold px-3" style="font-size: 0.7rem;">
                            <span>Poor</span>
                            <span>Excellent</span>
                        </div>
                        <div class="rating-group">
                            <% for (let i = 1; i <= 5; i++) { %>
                                <div>
                                    <input type="radio" class="rating-input" name="usefulness" id="use<%= i %>" value="<%= i %>" required>
                                    <label class="rating-label" for="use<%= i %>"><%= i %></label>
                                </div>
                            <% } %>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label for="comments" class="form-label fw-bold text-uppercase text-secondary" style="font-size: 0.75rem;">Additional Comments</label>
                        <textarea class="form-control" id="comments" name="comments" rows="3" placeholder="Share your thoughts..."></textarea>
                    </div>

                    <div class="d-grid mt-4">
                        <button type="submit" class="btn btn-primary py-2 shadow-sm">
                            Submit Feedback
                        </button>
                    </div>

                </form>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>