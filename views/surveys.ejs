<!DOCTYPE html>
<html lang="en">
<head>
    <title>Post-Event Survey - Ella Rises</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-light">

    <%- include('partials/header') %>

    <div class="container mt-5 mb-5" style="max-width: 800px;">
        <div class="card shadow-lg border-0">
            <div class="card-header bg-primary text-white py-4">
                <h2 class="mb-0 fw-bold" style="font-family: 'DM Serif Display', serif;">Program Feedback</h2>
                <p class="mb-0 opacity-75">Your feedback helps us improve future events.</p>
            </div>
            
            <div class="card-body p-5">
                <form action="/survey/submit" method="POST">
                    
                    <div class="mb-4">
                        <label for="eventName" class="form-label fw-bold">Which event did you attend?</label>
                        <select class="form-select" id="eventName" name="eventName" required>
                            <option value="" selected disabled>-- Select an Event --</option>
                            <% if (events && events.length > 0) { %>
                                <% events.forEach(e => { %>
                                    <option value="<%= e.eventname %>"><%= e.eventname %></option>
                                <% }) %>
                            <% } else { %>
                                <option value="General Program">General Program</option>
                            <% } %>
                        </select>
                    </div>

                    <hr>

                    <div class="mb-4">
                        <label class="form-label fw-bold d-block">Overall Satisfaction (1-5)</label>
                        <div class="btn-group w-100" role="group">
                            <% for (let i = 1; i <= 5; i++) { %>
                                <input type="radio" class="btn-check" name="satisfaction" id="sat<%= i %>" value="<%= i %>" required>
                                <label class="btn btn-outline-primary" for="sat<%= i %>"><%= i %></label>
                            <% } %>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label class="form-label fw-bold d-block">Usefulness of Content (1-5)</label>
                        <div class="btn-group w-100" role="group">
                            <% for (let i = 1; i <= 5; i++) { %>
                                <input type="radio" class="btn-check" name="usefulness" id="use<%= i %>" value="<%= i %>" required>
                                <label class="btn btn-outline-primary" for="use<%= i %>"><%= i %></label>
                            <% } %>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label for="comments" class="form-label fw-bold">Comments</label>
                        <textarea class="form-control" id="comments" name="comments" rows="3" maxlength="250"></textarea>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg">Submit Feedback</button>
                    </div>

                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>